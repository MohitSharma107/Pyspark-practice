#The below syntax is used to import the dataframe from the file accepted candidate from interviews.py

from accepted_candidate_from_interviews import df_final_selected
from account_balance import df_balance
from find_cutoff_score_for_each_school import df_join
from The_change_in_global_ranking import df_ranking 


df_final_selected.show()
df_balance.show()
df_join.show()  
df_ranking.show()